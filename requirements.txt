# Context-Aware Chatbot Dependencies
# ===================================
# Python 3.10+ required for modern type hints and async features
# Install: pip install -r requirements.txt

# Core Framework - Configuration and data validation
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# LLM Providers - API clients for language models
openai>=1.0.0,<2.0.0          # GPT-4, GPT-3.5-turbo support
anthropic>=0.18.0,<1.0.0      # Claude models (Haiku, Sonnet, Opus)
tiktoken>=0.5.0,<1.0.0        # Token counting for context management

# Embeddings & Vector Store - Semantic search and memory retrieval
sentence-transformers>=2.2.0,<3.0.0  # Generate text embeddings
faiss-cpu>=1.7.4,<2.0.0              # Efficient similarity search
numpy>=1.24.0,<2.0.0                 # Numerical operations

# Async Support - Rate limiting and concurrency control
asyncio-throttle>=1.0.0,<2.0.0

# Testing - Quality assurance and code coverage
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-cov>=4.0.0,<5.0.0

# Development - Code quality and environment management
python-dotenv>=1.0.0,<2.0.0  # Load environment variables from .env
black>=23.0.0,<25.0.0        # Code formatter
ruff>=0.1.0,<1.0.0           # Fast Python linter
mypy>=1.0.0,<2.0.0           # Static type checker

# Optional: FastAPI (for API server deployment)
# Uncomment to enable REST API endpoints
# fastapi>=0.100.0,<1.0.0
# uvicorn[standard]>=0.22.0,<1.0.0
